$ const props = {
  id: input.id,
  path: input.path,
  target: input.target,
  defaults: input.defaults,
  selectAllTargets: input.selectAllTargets,
};

<marko-web-browser-component name="GlobalRevealAdHandler" props=props />
